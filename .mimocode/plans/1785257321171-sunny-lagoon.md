# Plan: SVG transparency + audio-style buttons + scroll bug fix

## 3 changes

### 1. SVG more transparent

**File:** `src/styles/main.scss` line 490

- Change `.prime-hollow svg` opacity from `0.3` to `0.08`

### 2. All buttons styled like audio button (keep colored accents)

**File:** `src/styles/main.scss`

Audio button style key properties:

```scss
border: 2px solid #6b7280;
border-radius: 5px;
background: linear-gradient(145deg, #2a2a2a, #1a1a1a);
backdrop-filter: blur(10px);
box-shadow:
  0 4px 15px rgba(0, 0, 0, 0.3),
  inset 0 1px 0 rgba(255, 255, 255, 0.05);
```

Apply to these buttons with colored accent borders:

- `.prime-buttons__stream` — border-color: $purple-color
- `.prime-buttons__register` — border-color: $burgundy-light
- `.report-btn` — border-color: #5865f2 (Discord blurple)

Update `.prime-buttons a` base style to match audio button, remove flat backgrounds, add gradient + border. Override `border-color` per variant.

Update `.report-btn` similarly (border-radius: 0 5px 5px 0 since it sticks to left edge).

### 3. Fix scroll bug — poster 3 bg4 triggers too early when scrolling up

**File:** `src/components/ScrollBackgrounds.tsx`

Bug: Easter egg trigger has `onLeaveBack: () => setActiveLayer('bg4')`. When scrolling up from the easter egg, this fires before poster 3's trigger re-enters, causing an immediate switch to bg4 instead of waiting until poster 3 is actually visible.

Fix: Change easter egg's `onLeaveBack` to `() => setActiveLayer('video')` — the poster 3 trigger will handle switching to bg4 when it actually enters the viewport.

## Verification

1. `yarn dev` — open page
2. Scroll down — SVG behind PRIME section should be very faint (opacity 0.08)
3. Check buttons: gradient dark background, colored borders, pulse on hover like audio button
4. Scroll to easter egg, scroll back up — background should stay `video` until poster 3 zone is reached, then switch to bg4
